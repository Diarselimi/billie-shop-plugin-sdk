#!/usr/bin/env bash

# executes commands on a running container

if [[ -t 1 ]]; then
  # stdout is a terminal
  docker-compose exec -e TERM=${TERM:-xterm-256color} "${@}"
else
  # stdout is not a terminal
  docker-compose exec -T "${@}"
fi
