#!/usr/bin/env bash

if [[ -t 1 ]]; then
  # stdout is a terminal
  docker-compose exec --workdir /app -e TERM=${TERM:-xterm-256color} app "${@}"
else
  # stdout is not a terminal
  docker-compose exec --workdir /app -T app "${@}"
fi
