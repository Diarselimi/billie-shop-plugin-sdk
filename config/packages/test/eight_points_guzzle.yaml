eight_points_guzzle:
  clients:
    alfred:
      base_url: "%behat_mock_server_url%"
      options:
        query:
          app: alfred
    borscht:
      base_url: "%behat_mock_server_url%"
      options:
        query:
          app: borscht
    smaug:
      base_url: "%behat_mock_server_url%"
      options:
        query:
          app: smaug
